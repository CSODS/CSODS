import { Header, UpButton, ProjectsDisplay } from "./components";

export default function StudentProjects() {
  return (
    <div>
      <Header />
      <ProjectsDisplay />
      <UpButton />
    </div>
  );
}
