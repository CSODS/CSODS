import { Header, UpButton, ProjectsDisplay } from "./components";

export function StudentProjects() {
  return (
    <div>
      <Header />
      <ProjectsDisplay />
      <UpButton />
    </div>
  );
}
